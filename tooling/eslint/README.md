# ESLint Tooling

The active shared ESLint configs live in `packages/eslint-config`.

This folder documents the intended tooling boundary while keeping the reusable presets package-addressable inside the pnpm workspace.
