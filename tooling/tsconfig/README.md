# TypeScript Tooling

The active shared TypeScript configs live in `packages/typescript-config` so apps and packages can consume them through workspace dependencies.

This folder exists as the conventional tooling home for future expansion without coupling app code to local relative paths.
